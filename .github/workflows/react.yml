name: Build e Deploy React

on:
  push:
    branches:
      - 'main'

jobs:
  build-deploy:
    runs-on: ubuntu-latest
    steps:
    - name: a
      run: cd website
    - id: build-publish
      uses: bitovi/github-actions-react-to-ghp@v1.0.0
